import XCTest
@testable import AppCustomization

final class AppCustomizationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AppCustomization().text, "Hello, World!")
    }
}
